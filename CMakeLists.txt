include(cmakes/SETcmake.cmake)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
if(NOT CMAKE_TOOLCHAIN_FILE)
set(CMAKE_TOOLCHAIN_FILE "${CMAKE_SOURCE_DIR}/conan_cmakes/conan_toolchain.cmake")
endif()
cmake_minimum_required(VERSION 3.12)
cmake_policy(SET CMP0091 NEW)
project(
  PDJE
  VERSION 0.1
  LANGUAGES C CXX)


include(cmakes/Options.cmake)

set(RT_HASH_HEADER ${CMAKE_CURRENT_BINARY_DIR}/generated/PDJE_INPUT_PROCESS_HASH.hpp)
set(RT_HASH_HEADER_INCLUDE ${CMAKE_CURRENT_BINARY_DIR}/generated/)
set(genhash_template "// Auto-generated by GenerateHash.cmake\n#pragma once\nstatic const char EMBEDDED_INPUT_PROCESS_SHA256[] = \"\";\n")
file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/generated/)
file(WRITE "${RT_HASH_HEADER}" "${genhash_template}")

Include(FetchContent)
include(cmakes/findPackages.cmake)

if(WIN32)
  add_compile_definitions(__WINDOWS__)
  add_compile_options("$<$<CXX_COMPILER_ID:MSVC>:/utf-8>")
  
else()
endif()

set(PDJE_INCLUDE_ROOT  ${CMAKE_SOURCE_DIR} CACHE STRING "Set pdje root")

#for release + symbol
if(PDJE_BENCHMARK)
add_compile_definitions(PDJE_BENCHMARK_ON)
  if(WIN32)
    set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} /Zi")
    set(CMAKE_EXE_LINKER_FLAGS_RELEASE "${CMAKE_EXE_LINKER_FLAGS_RELEASE} /DEBUG")
  endif()
endif()
#end symbol

if(PDJE_DYNAMIC)
  if(WIN32)
    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>DLL")
  else()
    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
  endif()
endif()


function(DynamicInnerFlag target)
if(WIN32 AND PDJE_DYNAMIC)
    target_compile_definitions(${target} PRIVATE PDJE_WINDOWS_DLL)
    target_compile_definitions(${target} PRIVATE PDJE_BUILDING)
  endif()
endfunction(DynamicInnerFlag target)

function(DynamicOuterFlag target)
if(WIN32 AND PDJE_DYNAMIC)
    target_compile_definitions(${target} PUBLIC PDJE_WINDOWS_DLL)
  endif()
endfunction(DynamicOuterFlag target)

function(FacadeTargetFlags target)
  if(WIN32)
    target_compile_definitions(${target} PRIVATE PDJE_BUILDING)
    target_compile_definitions(${target} PUBLIC PDJE_WINDOWS_DLL)
  else()
    set_target_properties(${target} PROPERTIES
      CXX_VISIBILITY_PRESET hidden
      VISIBILITY_INLINES_HIDDEN ON
    )
  endif()
endfunction(FacadeTargetFlags)

function(LinuxSetAtomic targetName)
  if(LINUX)
    target_link_libraries(${targetName} PUBLIC atomic)
    target_link_options(${targetName} PUBLIC -Wl,-z,defs)
  endif()
endfunction()

function(LinuxSetAudioLibs targetName)
  if(LINUX)
    target_link_libraries(${targetName} PUBLIC asound)
  endif()
endfunction(LinuxSetAudioLibs )

include(cmakes/HalideSupport.cmake)


include(cmakes/BuildLog.cmake)
include(cmakes/includeDIRS.cmake)
include(cmakes/src/GLOBAL/GlobalPkg.cmake)

# Build Logging Lib

# Source Include
include(cmakes/src/CORE/CorePkg.cmake)

if(PDJE_DEVELOP_INPUT)
  include(cmakes/src/JUDGE/JudgePkg.cmake)
  include(cmakes/src/INPUT/InputPkg.cmake)
endif()

include(cmakes/src/UTIL/UtilPkg.cmake)



# include(cmakes/FunctionSet.cmake)


# target_include_directories(CRYPTO_OBJ PUBLIC ${PDJE_INCLUDE_IPC} ${PDJE_INCLUDE_GLOBAL})




# Build Subproc Target
if(PDJE_DEVELOP_INPUT AND WIN32)
  include(cmakes/BuildSubProc.cmake)
endif()


# Build Unit Tests

if(PDJE_TEST)
  include(CTest)
  enable_testing()
  include(cmakes/tests/UnitTestRoot.cmake)
endif()


# Build Dev Tests

if(PDJE_DEV_TEST)
  include(cmakes/tests/DevTests.cmake)
endif(PDJE_DEV_TEST)





# Build Main Targets

if(PDJE_DYNAMIC)
  add_library(PDJE SHARED ${CORE_SRC_EXPORT})
  set_target_properties(PDJE PROPERTIES CXX_VISIBILITY_PRESET hidden)
  set_target_properties(PDJE PROPERTIES VISIBILITY_INLINES_HIDDEN ON)
  if(UNIX AND NOT APPLE)
    target_link_options(PDJE PRIVATE "-Wl,--exclude-libs,ALL")
    # Force static linking of libstdc++ and libgcc into the shared library
    target_link_libraries(PDJE PRIVATE -static-libstdc++ -static-libgcc)
  endif()
else()
  add_library(PDJE ${CORE_SRC_EXPORT})
endif()
setCoreReqs(PDJE)
DynamicOuterFlag(PDJE)
target_link_libraries(PDJE PRIVATE CORE_OBJ)

add_library(CPDJE SHARED
  ${CMAKE_CURRENT_SOURCE_DIR}/include/core/interface/CPDJE_interface.cpp
)
target_include_directories(CPDJE
  PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/include/global
    ${CMAKE_CURRENT_SOURCE_DIR}/include/core/interface
  PRIVATE
    ${PDJE_INCLUDE_CORE}
    ${PDJE_INCLUDE_ROOT}/third_party/SoundTouch/soundtouch_include
)
target_link_libraries(CPDJE PRIVATE CORE_OBJ GLOBAL_OBJ PDJE_LOG_RUNTIME CORE_INCLUDE GLOBAL_INCLUDE IPC_INCLUDE MINIAUDIO_INCLUDE MINIAUDIO_OBJ SOUNDTOUCH_INCLUDE SOUNDTOUCH_OBJ SQL_INCLUDE SQL_OBJ)
setSpdlogReqLib(CPDJE)
setSqliteReqLib(CPDJE)
setRocksDBReqLib(CPDJE)
setLibgit2ReqLib(CPDJE)
setHighwayReqLib(CPDJE)
setCapnpReqLib(CPDJE)
pdje_halide_attach_to_target(CPDJE)
LinuxSetAtomic(CPDJE)
LinuxSetAudioLibs(CPDJE)
PDJE_COMPILE_OPTION(CPDJE)
SET_PROPERTIES(CPDJE)
FacadeTargetFlags(CPDJE)

if(PDJE_DEVELOP_INPUT)
  if(PDJE_DYNAMIC)
    add_library(PDJE_MODULE_JUDGE SHARED ${JUDGE_SRC_EXPORT})
    add_library(PDJE_MODULE_INPUT SHARED ${INPUT_SRC_EXPORT})
    set_target_properties(PDJE_MODULE_JUDGE PROPERTIES CXX_VISIBILITY_PRESET hidden)
    set_target_properties(PDJE_MODULE_JUDGE PROPERTIES VISIBILITY_INLINES_HIDDEN ON)
     
    set_target_properties(PDJE_MODULE_INPUT PROPERTIES CXX_VISIBILITY_PRESET hidden)
    set_target_properties(PDJE_MODULE_INPUT PROPERTIES VISIBILITY_INLINES_HIDDEN ON)
  else()
    add_library(PDJE_MODULE_JUDGE ${JUDGE_SRC_EXPORT})
    add_library(PDJE_MODULE_INPUT ${INPUT_SRC_EXPORT})
endif()# end dynamic
  DynamicOuterFlag(PDJE_MODULE_JUDGE)
  DynamicOuterFlag(PDJE_MODULE_INPUT)
  target_link_libraries(PDJE_MODULE_JUDGE PRIVATE 
  JUDGE_OBJ
  GLOBAL_OBJ
  IPC_OBJ
  CRYPTO_OBJ
  )
  
  target_link_libraries(PDJE_MODULE_INPUT PRIVATE 
  INPUT_OBJ
  GLOBAL_OBJ
  IPC_OBJ
  CRYPTO_OBJ
  )
  

  # setInputReqs(PDJE_MODULE_INPUT)
  # setJudgeReqs(PDJE_MODULE_JUDGE)

  add_library(CPDJE_MODULE_INPUT SHARED
    ${CMAKE_CURRENT_SOURCE_DIR}/include/input/CPDJE_Input.cpp
  )
  target_include_directories(CPDJE_MODULE_INPUT
    PUBLIC
      ${CMAKE_CURRENT_SOURCE_DIR}/include/global
      ${CMAKE_CURRENT_SOURCE_DIR}/include/input
    PRIVATE
      ${PDJE_INCLUDE_ROOT}/include/input/midi
  )
  target_link_libraries(CPDJE_MODULE_INPUT
    PRIVATE
      INPUT_OBJ
      GLOBAL_OBJ
      IPC_OBJ
      CRYPTO_OBJ
      PDJE_LOG_RUNTIME
      INPUT_MAIN_INCLUDE
      IPC_INCLUDE
      GLOBAL_INCLUDE
      CRYPTO_INCLUDE
  )
  setSpdlogReqLib(CPDJE_MODULE_INPUT)
  setBotanReqLib(CPDJE_MODULE_INPUT)
  setLibreMIDIReqLib(CPDJE_MODULE_INPUT)
  FacadeTargetFlags(CPDJE_MODULE_INPUT)

  add_library(CPDJE_MODULE_JUDGE SHARED
    ${CMAKE_CURRENT_SOURCE_DIR}/include/judge/CPDJE_Judge.cpp
  )
  target_include_directories(CPDJE_MODULE_JUDGE
    PUBLIC
      ${CMAKE_CURRENT_SOURCE_DIR}/include/global
      ${CMAKE_CURRENT_SOURCE_DIR}/include/core/interface
      ${CMAKE_CURRENT_SOURCE_DIR}/include/input
      ${CMAKE_CURRENT_SOURCE_DIR}/include/judge
    PRIVATE
      ${PDJE_INCLUDE_ROOT}/include/judge
  )
  target_link_libraries(CPDJE_MODULE_JUDGE
    PRIVATE
      JUDGE_OBJ
      GLOBAL_OBJ
      IPC_OBJ
      CRYPTO_OBJ
      PDJE_LOG_RUNTIME
      JUDGE_INCLUDE
      INPUT_MAIN_INCLUDE
      IPC_INCLUDE
      GLOBAL_INCLUDE
      CRYPTO_INCLUDE
  )
  setSpdlogReqLib(CPDJE_MODULE_JUDGE)
  setBotanReqLib(CPDJE_MODULE_JUDGE)
  setLibreMIDIReqLib(CPDJE_MODULE_JUDGE)
  FacadeTargetFlags(CPDJE_MODULE_JUDGE)
endif()





# Swig Option

if(PDJE_SWIG_BUILD)
  include(cmakes/ADD_SWIGS.cmake)
endif()


include(GNUInstallDirs)

pdje_halide_install_generated_targets()


install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/include/  DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
  FILES_MATCHING
    PATTERN "*.h" PATTERN "*.hpp" PATTERN "*.hxx" PATTERN "*.inl" PATTERN "*.ipp"
  PATTERN ".git*" EXCLUDE
  PATTERN "CMakeLists.txt" EXCLUDE)

